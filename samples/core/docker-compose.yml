services:

  mqtt:
    # https://hub.docker.com/_/eclipse-mosquitto/
    image: eclipse-mosquitto:2.1.2-alpine
    restart: unless-stopped
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
      - ./mosquitto/config:/mosquitto/config:ro
    networks:
      - ti-dhome

  nodered:
    image: nodered/node-red:5.0.7
    restart: unless-stopped
    environment:
      - TZ=Europe/Paris
    ports:
      - "1880:1880"
    volumes:
      - node-red-data:/data
    networks:
      - ti-dhome
    depends_on:
      - mqtt

  victoriametrics:
    # https://github.com/VictoriaMetrics/VictoriaMetrics/releases
    image: victoriametrics/victoria-metrics:v1.151.0
    restart: unless-stopped
    ports:
      - "8428:8428"
    command:
      - -retentionPeriod=3y
      - -storageDataPath=/victoria-metrics-data
    volumes:
      - victoria-metrics-data:/victoria-metrics-data
    networks:
      - ti-dhome
    depends_on:
      - mqtt

  vmbackup:
    # One-shot incremental backup via the snapshot API (run: make vm-backup)
    # https://docs.victoriametrics.com/victoriametrics/vmbackup/
    image: victoriametrics/vmbackup:v1.151.0
    restart: "no"
    profiles:
      - backup
    volumes:
      - victoria-metrics-data:/victoria-metrics-data
      - ./backup/vmbackup:/backup
      - vmbackup-tmp:/tmp   # scratch space for staging parts during a backup (see README)
    command:
      - -storageDataPath=/victoria-metrics-data
      - -snapshot.createURL=http://victoriametrics:8428/snapshot/create
      - -snapshot.deleteURL=http://victoriametrics:8428/snapshot/delete
      - -dst=fs:///backup
    networks:
      - ti-dhome
    depends_on:
      - victoriametrics

volumes:
  node-red-data:
  victoria-metrics-data:
  vmbackup-tmp:

networks:
  ti-dhome:
    name: ti-dhome